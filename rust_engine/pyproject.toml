[build-system]
requires = ["maturin>=1.0,<2"]
build-backend = "maturin"

[project]
name = "rust_engine"
version = "0.1.0"
description = "Rust engine for RustModelSerializer (PyO3 extension)"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
